#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/alena/historical_dates/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/alena/historical_dates/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../webpack@5.97.1_webpack-cli@6.0.1/node_modules/webpack/bin/webpack.js" "$@"
else
  exec node  "$basedir/../../../../../../webpack@5.97.1_webpack-cli@6.0.1/node_modules/webpack/bin/webpack.js" "$@"
fi
